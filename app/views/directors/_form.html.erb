<%= form_for(@director) do |f| %>
    <%= render partial: 'shared/errors',
             locals: { model: @director } %>

  <div class="field">
    <%= f.label :full_legal_name %><br>
    <%= f.text_field :full_legal_name %>
  </div>
  <div class="field">
    <%= f.label :occupation %><br>
    <%= f.text_field :occupation %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
